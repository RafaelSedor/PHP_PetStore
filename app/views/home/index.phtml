<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="/">PHP PetStore</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <?php if (isset($user)): ?>
                    <li class="nav-item">
                        <a class="nav-link" href="/user/<?= htmlspecialchars($user->id, ENT_QUOTES, 'UTF-8') ?>">
                            <i class="bi bi-person-circle"></i> Meu Perfil
                        </a>
                    </li>
                    <?php if ($user->role === 'admin'): ?>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a>
                        </li>
                    <?php endif; ?>
                    <li class="nav-item">
                        <a class="nav-link text-danger" href="/logout"><i class="bi bi-box-arrow-right"></i> Sair</a>
                    </li>
                <?php else: ?>
                    <li class="nav-item">
                        <a class="nav-link" href="/login"><i class="bi bi-box-arrow-in-right"></i> Entrar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/user/create"><i class="bi bi-person-plus"></i> Criar Conta</a>
                    </li>
                <?php endif; ?>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <div class="text-center">
        <h1 class="mb-4">Bem-vindo ao <span class="text-primary">PHP PetStore</span></h1>
        <?php if (isset($user)): ?>
            <p class="fs-4">Olá, <strong><?= htmlspecialchars($user->name, ENT_QUOTES, 'UTF-8') ?></strong>!</p>
        <?php else: ?>
            <p class="fs-5">Faça login para acessar todas as funcionalidades ou crie uma conta para começar.</p>
        <?php endif; ?>
    </div>
</div>