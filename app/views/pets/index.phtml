<div class="container mt-5">
    <h1 class="text-primary text-center">Meus Pets</h1>
    <div class="text-end mb-3">
        <a href="/pets/create" class="btn btn-success"><i class="bi bi-plus-circle"></i> Adicionar Pet</a>
    </div>
    <ul class="list-group">
        <?php foreach ($pets as $pet): ?>
            <li class="list-group-item d-flex justify-content-between align-items-center">
                <span>
                    <strong><?= htmlspecialchars($pet->name) ?></strong> (<?= htmlspecialchars($pet->species) ?>, <?= htmlspecialchars($pet->age) ?> anos)
                </span>
                <div>
                    <a href="/pets/<?= $pet->id ?>/edit" class="btn btn-warning btn-sm me-2"><i class="bi bi-pencil-square"></i> Editar</a>
                    <form action="/pets/<?= $pet->id ?>/delete" method="POST" style="display:inline;">
                        <button type="submit" class="btn btn-danger btn-sm"><i class="bi bi-trash"></i> Excluir</button>
                    </form>
                </div>
            </li>
        <?php endforeach; ?>
    </ul>
    <div class="mt-4">
        <a href="/" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Voltar</a>
    </div>
</div>