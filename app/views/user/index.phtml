<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <h1 class="text-center text-primary mb-4">Lista de Usuários</h1>

            <div class="text-end mb-3">
                <a href="/user/create" class="btn btn-success">
                    <i class="bi bi-plus-circle"></i> Novo Usuário
                </a>
            </div>

            <ul class="list-group">
                <?php foreach ($users as $user): ?>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span>
                            <strong><?= htmlspecialchars($user->name) ?></strong>
                            (<?= htmlspecialchars($user->email) ?>)
                        </span>

                        <div>
                            <a href="/user/<?= $user->id ?>" class="btn btn-info btn-sm me-2">
                                <i class="bi bi-eye"></i> Ver
                            </a>
                            <a href="/user/<?= $user->id ?>/edit" class="btn btn-warning btn-sm me-2">
                                <i class="bi bi-pencil-square"></i> Editar
                            </a>
                            <form action="/user/<?= $user->id ?>/delete" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-danger btn-sm">
                                    <i class="bi bi-trash"></i> Excluir
                                </button>
                            </form>
                        </div>
                    </li>
                <?php endforeach; ?>
            </ul>
        </div>
    </div>
</div>